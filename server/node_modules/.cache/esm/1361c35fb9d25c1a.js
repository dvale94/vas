let validator,isEmpty;_b9d‍.x([["default",()=>_b9d‍.o]]);_b9d‍.w("validator",[["default",["validator"],function(v){validator=v}]]);_b9d‍.w("is-empty",[["default",["isEmpty"],function(v){isEmpty=v}]]);


function validateLoginInput(data) {

    let errors = {};

    // convert empty fields to an empty string so we can use validator functions
	data.email = !isEmpty(data.username) ? data.username : '';
    data.password = !isEmpty(data.password) ? data.password : '';
    
    // email checks
	if (Validator.isEmpty(data.email)) {
		errors.email = 'Email field is required';
    } 
    else if (!validator.isEmail(data.email)) {
        errors.email = 'Email is invalid';
    }

    // password checks
	if (Validator.isEmpty(data.password)) {
		errors.password = 'Password field is required';
    }
    
    return {
		errors,
		isValid: isEmpty(errors)
	};
}

_b9d‍.d(validateLoginInput());