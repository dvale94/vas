let validator,isEmpty;_e38‍.x([["default",()=>_e38‍.o]]);_e38‍.w("validator",[["default",["validator"],function(v){validator=v}]]);_e38‍.w("is-empty",[["default",["isEmpty"],function(v){isEmpty=v}]]);


function validateLoginInput(data) {

    let errors = {};

    // convert empty fields to an empty string so we can use validator functions
	data.email = !isEmpty(data.email) ? data.email : '';
    data.password = !isEmpty(data.password) ? data.password : '';
    
    // email checks
	if (validator.isEmpty(data.email)) {
		errors.email = 'Email field is required';
    } 
    else if (!validator.isEmail(data.email)) {
        errors.password = 'Email and/or Password invalid';
    }

    // password checks
	if (validator.isEmpty(data.password)) {
		errors.password = 'Password field is required\n';
    }
    
    
    return {
		errors,
		isValid: isEmpty(errors)
	};
};

_e38‍.d(validateLoginInput);